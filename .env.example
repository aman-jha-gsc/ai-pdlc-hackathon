# This is the connection string for your PostgreSQL database.
# It should be in the format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For the provided docker-compose.yml, this would be:
# DATABASE_URL="postgresql://user:password@localhost:5432/zenith"
DATABASE_URL="postgresql://user:password@localhost:5432/zenith"
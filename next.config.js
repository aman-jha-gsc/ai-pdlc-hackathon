/** @type {import('next').NextConfig} */
const nextConfig = {
    // React's Strict Mode is recommended for new Next.js apps.
    // It helps highlight potential problems in an application.
    reactStrictMode: true,
};

module.exports = nextConfig;